@model IEnumerable<disavow_1.Models.Disavow>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Url)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CheckedDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NoFollowNoLink)
            </th>
            <th>
                @*@Html.DisplayNameFor(model => model.Parsed)*@
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Url)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CheckedDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NoFollowNoLink)
            </td>
            <td>
                <form asp-action="crawler" asp-route-id="@item.Id">
                    <button >Parse</button>
                </form>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
